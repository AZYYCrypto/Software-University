function calcSumFromFirstAndLast(arrOfStr) {
    let firstNum = Number(arrOfStr[0]);
    let lastNum = Number(arrOfStr[arrOfStr.length - 1]);

    console.log(firstNum + lastNum);
    
}

calcSumFromFirstAndLast(['20', '30', '40']);
