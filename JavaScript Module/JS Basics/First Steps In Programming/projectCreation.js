function projectCreation(input) {
        let nameArchitect = input[0];
        let numOfProject = Number(input[1]);
        let sumHours = 3 * numOfProject;

        console.log(`The architect ${nameArchitect} will need ${sumHours} hours to complete ${numOfProject} project/s.`)

}

